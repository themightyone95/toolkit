machine:
  node:
    version: 8.9.4

general:
  artifacts:
    - "build/"
    - "package.json"

test:
  override:
    - npm run circle

experimental:
  notify:
    branches:
      only:
        - master
